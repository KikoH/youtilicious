<div id="wrapper">
  <div id="columns">
  <% @items.each do |item| %>
    <div class="pin">
      <img src="<%= item.image_url %>" />
      <p>
        <%= item.description %>
      </p>
    </div>
    <% end %>
  </div>
</div>